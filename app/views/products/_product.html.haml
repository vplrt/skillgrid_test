.box.panel.panel-default
  = link_to (image_tag product.image.url), product
  .panel-body
    -if product.pro
      .pro.pull-right Pro
    %h2= link_to product.title, product
    - if seller_signed_in? || admin_signed_in?
      %p
        Seller:
        = product.company
